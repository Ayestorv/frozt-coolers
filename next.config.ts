import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Remove the i18n config here as App Router handles it differently
  // We'll implement i18n routing in the app directory instead
};

export default nextConfig;
